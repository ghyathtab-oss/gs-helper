/* ===== GS Helper – Shared Styles ===== */
:root {
  --bg-1: #1e3a8a;
  --bg-2: #4338ca;
  --bg-3: #6d28d9;
  --grad-primary: linear-gradient(135deg, #2563eb 0%, #06b6d4 100%);
  --grad-accent: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  --grad-female: linear-gradient(135deg, #ec4899 0%, #f472b6 100%);
  --grad-male: linear-gradient(135deg, #2563eb 0%, #0ea5e9 100%);
  --glass-bg: rgba(255, 255, 255, 0.92);
  --glass-border: rgba(255, 255, 255, 0.35);
  --text-dark: #0f172a;
  --text-muted: #64748b;
  --text-light: #f8fafc;
  --shadow-sm: 0 4px 16px rgba(15, 23, 42, 0.08);
  --shadow-md: 0 10px 30px rgba(15, 23, 42, 0.15);
  --shadow-lg: 0 20px 50px rgba(15, 23, 42, 0.25);
  --radius: 20px;
  --radius-sm: 14px;
  --transition: 0.28s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

html, body {
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Tajawal', 'Cairo', system-ui, sans-serif;
  direction: rtl;
  min-height: 100vh;
  background: linear-gradient(160deg, var(--bg-1) 0%, var(--bg-2) 45%, var(--bg-3) 100%);
  background-attachment: fixed;
  color: var(--text-dark);
  padding-bottom: 92px;
  position: relative;
  overflow-x: hidden;
}

/* decorative blobs */
body::before,
body::after {
  content: "";
  position: fixed;
  border-radius: 50%;
  filter: blur(80px);
  opacity: 0.5;
  z-index: 0;
  pointer-events: none;
}
body::before {
  width: 320px; height: 320px;
  background: #06b6d4;
  top: -100px; right: -80px;
}
body::after {
  width: 360px; height: 360px;
  background: #8b5cf6;
  bottom: 40px; left: -120px;
}

/* ===== Container ===== */
.app {
  max-width: 480px;
  margin: 0 auto;
  padding: 0 16px;
  position: relative;
  z-index: 1;
}

/* ===== Header (glass, blur) ===== */
.app-header {
  position: sticky;
  top: 0;
  z-index: 50;
  max-width: 480px;
  margin: 0 auto 18px;
  padding: 18px 20px;
  border-radius: 0 0 24px 24px;
  background: rgba(15, 23, 42, 0.55);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border: 1px solid rgba(255, 255, 255, 0.12);
  box-shadow: var(--shadow-md);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
}
.app-header .title-block h1 {
  color: var(--text-light);
  font-size: 1.15rem;
  font-weight: 800;
  line-height: 1.3;
}
.app-header .title-block p {
  color: rgba(255, 255, 255, 0.65);
  font-size: 0.7rem;
  letter-spacing: 1.5px;
  margin-top: 3px;
}
.header-btn {
  flex-shrink: 0;
  width: 46px; height: 46px;
  border-radius: 14px;
  background: var(--grad-primary);
  border: none;
  display: grid;
  place-items: center;
  color: #fff;
  font-size: 1.1rem;
  cursor: pointer;
  box-shadow: 0 8px 20px rgba(37, 99, 235, 0.45);
  transition: transform var(--transition);
}
.header-btn:active { transform: scale(0.92); }

/* ===== Cards ===== */
.card {
  background: var(--glass-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius);
  padding: 22px;
  margin-bottom: 18px;
  box-shadow: var(--shadow-md);
  animation: rise 0.5s ease both;
}
@keyframes rise {
  from { opacity: 0; transform: translateY(16px); }
  to { opacity: 1; transform: translateY(0); }
}
.card-title {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1.05rem;
  font-weight: 800;
  margin-bottom: 16px;
  color: var(--text-dark);
}
.card-title::before {
  content: "";
  width: 6px; height: 22px;
  border-radius: 6px;
  background: var(--grad-primary);
}
.card-sub {
  color: var(--text-muted);
  font-size: 0.85rem;
  margin-bottom: 14px;
  line-height: 1.6;
}

/* clickable nav cards (home) */
.nav-card {
  display: flex;
  align-items: center;
  gap: 16px;
  cursor: pointer;
  text-decoration: none;
  color: inherit;
  transition: transform var(--transition), box-shadow var(--transition);
}
.nav-card:hover { transform: translateY(-4px); box-shadow: var(--shadow-lg); }
.nav-card .icon-box {
  width: 58px; height: 58px;
  border-radius: 16px;
  display: grid;
  place-items: center;
  font-size: 1.5rem;
  color: #fff;
  flex-shrink: 0;
}
.nav-card .nc-text h3 { font-size: 1.05rem; font-weight: 800; }
.nav-card .nc-text p { color: var(--text-muted); font-size: 0.82rem; margin-top: 3px; }
.nav-card .arrow { margin-inline-start: auto; color: var(--text-muted); font-size: 1.1rem; }

/* ===== Forms ===== */
.field { margin-bottom: 14px; }
.field label {
  display: block;
  font-size: 0.8rem;
  font-weight: 700;
  color: var(--text-muted);
  margin-bottom: 7px;
}
.input, .select, textarea.input {
  width: 100%;
  padding: 14px 16px;
  border: 1.5px solid #e2e8f0;
  border-radius: var(--radius-sm);
  font-family: inherit;
  font-size: 0.95rem;
  background: #fff;
  color: var(--text-dark);
  transition: border-color var(--transition), box-shadow var(--transition);
  text-align: right;
}
.input:focus, .select:focus, textarea.input:focus {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.12);
}
textarea.input { resize: vertical; min-height: 80px; }

.search-box {
  position: relative;
  margin-bottom: 16px;
}
.search-box .input { padding-inline-start: 44px; }
.search-box .s-icon {
  position: absolute;
  inset-inline-start: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  font-size: 1rem;
}

/* ===== Buttons ===== */
.btn {
  width: 100%;
  padding: 15px;
  border: none;
  border-radius: var(--radius-sm);
  font-family: inherit;
  font-size: 0.98rem;
  font-weight: 800;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  transition: transform var(--transition), box-shadow var(--transition), opacity var(--transition);
}
.btn:active { transform: scale(0.97); }
.btn-primary {
  background: var(--grad-primary);
  color: #fff;
  box-shadow: 0 10px 24px rgba(37, 99, 235, 0.4);
}
.btn-accent {
  background: var(--grad-accent);
  color: #fff;
  box-shadow: 0 10px 24px rgba(139, 92, 246, 0.4);
}
.btn-ghost {
  background: rgba(37, 99, 235, 0.08);
  color: #2563eb;
}
.btn-sm {
  width: auto;
  padding: 10px 16px;
  font-size: 0.82rem;
  border-radius: 12px;
}
.btn-green { background: linear-gradient(135deg, #16a34a, #22c55e); color: #fff; }
.btn-gray { background: #f1f5f9; color: var(--text-dark); }

/* ===== Resident cards ===== */
.list { display: grid; gap: 14px; }
.resident-card {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius);
  padding: 16px;
  box-shadow: var(--shadow-sm);
  display: flex;
  align-items: center;
  gap: 14px;
  transition: transform var(--transition), box-shadow var(--transition);
  animation: rise 0.4s ease both;
}
.resident-card:hover { transform: translateY(-3px); box-shadow: var(--shadow-md); }
.avatar {
  width: 62px; height: 62px;
  border-radius: 18px;
  flex-shrink: 0;
  object-fit: cover;
  border: 2px solid #fff;
  box-shadow: var(--shadow-sm);
}
.avatar.male { background: var(--grad-male); }
.avatar.female { background: var(--grad-female); }
.r-info { flex: 1; min-width: 0; }
.r-info h3 { font-size: 1rem; font-weight: 800; }
.r-info .en { color: var(--text-muted); font-size: 0.8rem; }
.r-badge {
  display: inline-block;
  margin-top: 5px;
  padding: 3px 10px;
  border-radius: 8px;
  font-size: 0.72rem;
  font-weight: 800;
  background: rgba(37, 99, 235, 0.1);
  color: #2563eb;
}
.r-badge.female { background: rgba(236, 72, 153, 0.12); color: #db2777; }
.r-phone { color: var(--text-muted); font-size: 0.8rem; margin-top: 4px; direction: ltr; text-align: right; }
.r-actions { display: flex; flex-direction: column; gap: 8px; }
.icon-btn {
  width: 42px; height: 42px;
  border-radius: 12px;
  border: none;
  display: grid;
  place-items: center;
  cursor: pointer;
  font-size: 1rem;
  color: #fff;
  text-decoration: none;
  transition: transform var(--transition);
}
.icon-btn:active { transform: scale(0.9); }
.icon-call { background: var(--grad-primary); }
.icon-wa { background: linear-gradient(135deg, #25d366, #128c7e); }

/* ===== Patient result card ===== */
.patient-card {
  background: #fff;
  border: 1px solid #eef2f7;
  border-radius: var(--radius-sm);
  padding: 16px;
  margin-bottom: 12px;
  box-shadow: var(--shadow-sm);
  animation: rise 0.4s ease both;
}
.patient-card h3 { font-size: 1rem; font-weight: 800; margin-bottom: 4px; }
.patient-card .row { display: flex; gap: 6px; font-size: 0.85rem; margin-top: 5px; }
.patient-card .row .k { color: var(--text-muted); font-weight: 700; min-width: 88px; }
.patient-card .row .v { color: var(--text-dark); }

/* ===== Term cards ===== */
.term-card {
  background: #fff;
  border-radius: var(--radius-sm);
  padding: 16px;
  margin-bottom: 12px;
  box-shadow: var(--shadow-sm);
  border-inline-start: 5px solid #2563eb;
  animation: rise 0.4s ease both;
}
.term-card .en { font-size: 1.05rem; font-weight: 800; color: #1e3a8a; direction: ltr; text-align: left; }
.term-card .ar { font-size: 1rem; font-weight: 700; color: #2563eb; margin-top: 2px; }
.term-card .def { color: var(--text-dark); font-size: 0.88rem; margin-top: 8px; line-height: 1.6; }
.term-card .ex { color: var(--text-muted); font-size: 0.82rem; margin-top: 8px; background: #f8fafc; padding: 8px 12px; border-radius: 10px; line-height: 1.6; }

/* ===== About groups ===== */
.pgy-group { margin-bottom: 20px; }
.pgy-head {
  display: inline-block;
  padding: 6px 16px;
  border-radius: 12px;
  background: var(--grad-accent);
  color: #fff;
  font-weight: 800;
  font-size: 0.9rem;
  margin-bottom: 12px;
  box-shadow: 0 6px 16px rgba(139, 92, 246, 0.35);
}
.about-card {
  background: #fff;
  border-radius: var(--radius-sm);
  padding: 12px 16px;
  margin-bottom: 10px;
  box-shadow: var(--shadow-sm);
  display: flex;
  align-items: center;
  gap: 12px;
}
.about-card .a-avatar {
  width: 46px; height: 46px;
  border-radius: 12px;
  flex-shrink: 0;
}
.about-card h4 { font-size: 0.95rem; font-weight: 800; }
.about-card .en { color: var(--text-muted); font-size: 0.78rem; direction: ltr; text-align: right; }

/* ===== Bottom Navigation ===== */
.bottom-nav {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  z-index: 60;
  max-width: 480px;
  margin: 0 auto;
  padding: 10px 8px calc(10px + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border-top: 1px solid rgba(255, 255, 255, 0.5);
  box-shadow: 0 -8px 30px rgba(15, 23, 42, 0.15);
  display: flex;
  justify-content: space-around;
}
.nav-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 6px 0;
  text-decoration: none;
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 700;
  transition: color var(--transition), transform var(--transition);
}
.nav-item svg { width: 22px; height: 22px; }
.nav-item.active { color: #2563eb; }
.nav-item.active svg { transform: translateY(-2px); }
.nav-item:active { transform: scale(0.9); }

/* ===== Toast / messages ===== */
.toast {
  position: fixed;
  bottom: 110px;
  left: 50%;
  transform: translateX(-50%) translateY(20px);
  background: rgba(15, 23, 42, 0.92);
  color: #fff;
  padding: 12px 22px;
  border-radius: 14px;
  font-size: 0.88rem;
  font-weight: 700;
  z-index: 200;
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--transition), transform var(--transition);
  box-shadow: var(--shadow-lg);
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
.toast.error { background: rgba(220, 38, 38, 0.95); }
.toast.success { background: rgba(22, 163, 74, 0.95); }

.empty {
  text-align: center;
  color: var(--text-muted);
  padding: 30px 10px;
  font-size: 0.9rem;
}

/* ===== Login overlay ===== */
.login-overlay {
  position: fixed;
  inset: 0;
  z-index: 999;
  background: linear-gradient(160deg, var(--bg-1) 0%, var(--bg-2) 45%, var(--bg-3) 100%);
  display: grid;
  place-items: center;
  padding: 20px;
}
.login-box {
  width: 100%;
  max-width: 360px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(16px);
  border: 1px solid var(--glass-border);
  border-radius: 26px;
  padding: 34px 26px;
  box-shadow: var(--shadow-lg);
  text-align: center;
  animation: rise 0.5s ease both;
}
.login-box .logo {
  width: 72px; height: 72px;
  margin: 0 auto 16px;
  border-radius: 22px;
  background: var(--grad-primary);
  display: grid;
  place-items: center;
  font-size: 2rem;
  color: #fff;
  box-shadow: 0 12px 28px rgba(37, 99, 235, 0.45);
}
.login-box h2 { font-size: 1.25rem; font-weight: 800; margin-bottom: 6px; }
.login-box p { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 20px; }
.login-box .input { text-align: center; margin-bottom: 14px; }
.hidden { display: none !important; }

/* ===== Date / clock widget ===== */
.clock-card {
  text-align: center;
  background: var(--grad-primary);
  color: #fff;
  border: none;
}
.clock-card .time { font-size: 2.6rem; font-weight: 800; letter-spacing: 2px; direction: ltr; }
.clock-card .date { font-size: 0.95rem; opacity: 0.95; margin-top: 6px; }

/* ===== Modal ===== */
.modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 300;
  background: rgba(15, 23, 42, 0.6);
  backdrop-filter: blur(4px);
  display: grid;
  place-items: center;
  padding: 20px;
}
.modal-box {
  width: 100%;
  max-width: 340px;
  background: #fff;
  border-radius: 22px;
  padding: 26px;
  box-shadow: var(--shadow-lg);
  animation: rise 0.4s ease both;
}
.modal-box h3 { font-size: 1.1rem; font-weight: 800; margin-bottom: 6px; }
.modal-box p { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 16px; }
.modal-actions { display: flex; gap: 10px; margin-top: 6px; }

@media (max-width: 380px) {
  .app-header .title-block h1 { font-size: 1rem; }
  .clock-card .time { font-size: 2.2rem; }
}